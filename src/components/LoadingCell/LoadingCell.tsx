import LoadingIndicator from '../LoadingIndicator';
import * as styled from './styled';

const LoadingCell = () => {
	return (
		<styled.LoadingCell>
			<LoadingIndicator />
		</styled.LoadingCell>
	);
};

export default LoadingCell;
